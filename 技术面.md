## 8.13 泰则半导体

---

- 主要处理了哪些后端问题,可以给个例子吗
- yolov7 优化主要做了哪些,是前端吗
- 指令融合的2种都要会，isel和global都要了解
- 指令融合发生的时机也都要会,发生在isel和后面的一个阶段,区别是什么为什么需要
- 编译前中后端都有什么过程
- 编译中端的优化有哪些
- 寄存器分配有哪些算法
- 讲一讲bb,还有支配,被支配
- 讲一讲图着色算法
- 讲一讲SSA
- 如果遇见分支了SSA怎么办
- SIMT和SIMD的区别
- 讲一讲活跃度分析的那个算法
- 为什么公共层有你这个阶段的调度你还要添加这个调度,你这个调度和公共层的有什么区别
- 为什么需要多阶段
- 你是怎么做med3的融合的
- 了不了解ai编译
- 寄存器压力感知的调度的优化之后的效果大约是多少
- 会用ai嘛 市面上常用的都用过嘛
- 动态链接和静态链接的区别
- AOT和JIT的区别
- C++变成可执行文件的过程
- 现在主流的模型都了解吗,transformer了解吗

##### 计算机基础

- 虚函数
- Public protect private
- 类继承模版
- 智能指针
- 二叉搜索树
- 链表 数组的区别 增删改查的时间复杂度
- 快排,快排元素的选取



### 秋招应答的核心思路:

### 定义 → 核心思想 → 为什么这么设计 → 举例



##### GPT问我的

---

- Clang 和 LLVM 是什么关系

~~~~
Clang 和 LLVM 不是同一个东西。
Clang 主要是 C、C++、Objective-C 等语言的前端，负责词法分析、语法分析、语义分析，并最终生成 LLVM IR。
LLVM Core 提供 LLVM IR、优化框架以及代码生成等基础设施。
所以简单理解就是：
C/C++ → Clang → LLVM IR → LLVM optimizer/backend → machine code。
~~~~

- LLVM IR 是什么？为什么需要 IR？

~~~
LLVM IR 是 LLVM 使用的中间表示，它位于高级语言和目标机器代码之间。
LLVM IR 是一种低级、强类型并且基于 SSA 的 IR。使用 IR 的主要意义是把前端和后端解耦，同时让大量与具体源语言、目标架构无关的优化能够统一在 IR 层完成。
LLVM IR 同时存在文本形式、内存中的 C++ 对象形式以及 bitcode 二进制形式。

补充:
它们表达的是基本相同的 LLVM IR 程序，只是“承载方式”不同。
.ll：给人看的文本表示
内存 C++ 对象：LLVM 程序运行时真正操作的数据结构
.bc：适合保存和传输的紧凑二进制序列化表示

同一个学生的信息

define i32 @add(i32 %a, i32 %b) {
entry:
  %result = add i32 %a, %b
  ret i32 %result
}

对象模式会构造类似的对象关系然后保存
llvm::Module
    │
    └── llvm::Function   @add
            │
            └── llvm::BasicBlock   entry
                    │
                    ├── llvm::BinaryOperator
                    │       add %a, %b
                    │
                    └── llvm::ReturnInst
                            ret %result

bc:
42 43 C0 DE ...
~~~

==也就是说 IR 在内存里其实形成了一张：相互连接的对象图。==

- LLVM IR 的层级是什么？

~~~
Module
  ↓
Function
  ↓
BasicBlock
  ↓
Instruction

LLVM IR 从大的层级来看主要是 Module、Function、BasicBlock 和 Instruction。一个 Module 包含多个 Function，一个 Function 包含多个 BasicBlock，一个 BasicBlock 中由一系列 Instruction 组成。
~~~

- 什么是 Basic Block

~~~
Basic Block，也就是基本块，是一段单入口、单出口的顺序执行指令序列。
在一个基本块内部，除了末尾的 terminator 之外不存在控制流跳转；一旦进入这个基本块，中间的指令会依次执行到 terminator。
Basic Block 之间通过 branch 等 terminator 连接，从而组成 CFG，也就是控制流图。
~~~

- 控制流图CFG

~~~
节点BB、边:可能发生的控制流转移
~~~

- ==什么是SSA==

~~~
静态但复制
SSA 的核心特点是：每一个 SSA value 在静态程序中只会被定义一次。
如果源程序中的一个变量被多次赋值，在转换成 SSA 以后通常会被拆成多个不同版本，比如 x1、x2、x3。
当不同控制流路径产生的值在一个基本块汇合时，可以使用 PHI 节点根据控制流来源选择对应的值。
SSA 的好处是 def-use 关系非常明确，很多数据流分析和优化，比如常量传播、死代码消除、GVN 等都会更容易实现。
~~~

==易错点:==

SSA 中每个变量只能使用一次。(错误)

一个 SSA value 可以使用很多次。!!!(正确)

- ==SSA 把一个模糊的“变量在不同时间是什么值”的问题，变成了非常清晰的 def-use 数据依赖问题。所以方便优化==
- 有 if/else 时，SSA 怎么办？

~~~
用phi节点
~~~

- 什么是 PHI 节点？

~~~
PHI 节点主要用于 SSA 形式下控制流汇合时的值合并。
它会根据控制流是从哪个 predecessor basic block 到达当前基本块，选择对应的 incoming value。
比如 if 分支中 then 得到 x1，else 得到 x2，在 merge block 中可以通过 PHI 得到新的 SSA value x3。
PHI 本质上描述的是控制流边上的值选择，并不是说运行时一定真的存在一条“PHI 机器指令”；后端会把它进一步 lowering。
~~~

==phi的判别依据==:不是重新计算cond而是控制流实际上从哪个 predecessor edge 来。(\*\*\*\*)

- Dominator

~~~
在 CFG 中，如果从函数入口 Entry 到基本块 B 的每一条路径都必须经过基本块 A，那么称 A dominates B，也就是 A 支配 B。
~~~

~~~
一个节点的 dominators，是它所有前驱节点 dominator 集合的交集，再加上自己。
严格支配是指支配但是不是节点本身的节点
~~~

~~~
Immediate Dominator：idom
idom(B) 是严格支配 B 的节点中，距离 B 最近的那个。
因为所有节点的 idom 连起来，会形成一棵树：
Dominator Tree 
Dominator Tree 不是 CFG！
~~~

- Dominance Frontier 是什么？

~~~
Dominance Frontier 表示“某个节点的支配关系刚刚失效的位置”。
Dominance Frontier 可以理解为一个节点的支配关系刚开始失效的位置。比如一个 block A 能支配某条进入 B 的路径，但不能支配整个 B，那么 B 就可能位于 A 的 Dominance Frontier。它最典型的用途就是 SSA 构造时帮助确定 PHI 节点的插入位置。
~~~

~~~
SSA
 ↓
PHI
 ↓
Dominator
 ↓
Dominance Frontier
 ↓
PHI Insertion
 ↓
Variable Rename

~~~

- GVN 和 CSE 有什么区别？

~~~
GVN是全局值编号.它是编译器里用来发现“不同表达式其实算的是同一个值”的一类优化技术，最常见的效果就是消除重复计算。
GVN，全称 Global Value Numbering，是一种基于值等价关系消除冗余计算的优化。它会给程序中的值或表达式建立 value number，如果两个表达式在当前控制流和内存语义下能证明计算结果相同，就可以认为它们具有相同的 value number，从而复用之前已经计算出的结果。相比局部 CSE，GVN 可以跨基本块发现冗余，因此是全局优化。


CSE是公共子表达式删除
CSE 更强调“发现重复表达式并消除”，而 Value Numbering 更强调通过给等价值建立 value number 来证明值等价。GVN 则将这种值编号分析扩展到全局控制流范围，因此能够跨基本块发现冗余。
~~~

- 什么是 LLVM Pass？

~~~
Pass 可以理解为对 LLVM IR 执行的一次分析或者变换。
Analysis Pass 主要收集 IR 的信息，比如 Dominator Tree、Loop Info、Alias Analysis 等，而 Transformation Pass 会实际修改 IR，比如 DCE、InstCombine、Loop Optimization 等。
多个 Pass 会按照一定顺序组成 optimization pipeline，由 Pass Manager 负责组织执行，并管理 Analysis 的依赖、缓存和失效等问题。

~~~

- Analysis Pass 和 Transformation Pass

~~~
DominatorTree:不一定修改程序,但是回答哪个基本块支配哪个基本块

DCE:Dead Code Elimination 真的删掉代码。
~~~

- Pass Manager 是干嘛的？

~~~
Pass Manager 负责组织和执行整个 Pass pipeline，同时处理不同粒度的 Pass，以及 Analysis 的获取、缓存和失效问题。
~~~

有待补充



- LLVM 有哪些常见优化
  - Constant Folding
    - ![截屏2026-08-14 17.14.47](/Users/dawnsunlight/Library/Application Support/typora-user-images/截屏2026-08-14 17.14.47.png)
  - 死代码消除:后面用不到的直接删掉(????怎么判断用到还是用不到呢)
  - mem2reg
    - 把适合提升的栈上局部变量从 `alloca/load/store` 形式提升成 SSA register value。
    - ==不直接构建SSA的原因==:可以，但对有可变变量的命令式语言来说直接维护 SSA 比较麻烦。一个常见做法是先把局部变量表示成 `alloca + load/store` 的 memory form，再通过优化提升为 SSA form，这样前端实现会简单很多。
  - CSE / GVN
  - LICM:循环不变量外提
  - 循环展开:好处包括减少循环控制开销，并可能暴露更多优化机会。
  - Inlining:减少 call 开销。Inlining 往往能暴露新的跨过程优化机会。

- LLVM Backend 大致经过什么流程？

~~~
LLVM 后端负责把相对 target-independent 的 LLVM IR 转成具体目标机器代码。
整体会涉及指令选择，把 LLVM IR 映射成目标机器指令；之后处理目标合法化、机器级优化、寄存器分配、指令调度等过程，最后进行机器码或者汇编输出。
不同 Target 会提供自己的指令、寄存器、calling convention 等描述，其中 LLVM 的 TableGen 被广泛用于描述这些 target-specific 信息。
~~~

~~~
LLVM IR
   ↓
Instruction Selection
   ↓
Machine-level Representation
   ↓
Machine Optimization
   ↓
Register Allocation
   ↓
Instruction Scheduling
   ↓
Assembly / Machine Code

~~~

- JIT是什么

~~~
AOT 是在程序运行之前提前完成编译，生成可以直接运行的目标代码；JIT 则是在程序运行期间进行代码生成。
JIT 可以利用运行时已经知道的信息进行编译，并且适合动态加载或者动态语言等场景，不过代价是运行过程中本身需要承担编译开销。
LLVM 提供 ORC JIT 基础设施来支持 JIT compilation。

AOT

source
 ↓
compile
 ↓
machine code
 ↓
run


JIT

source / IR
 ↓
run
 ↓
compile while running
 ↓
machine code
 ↓
execute

~~~

- CFG 是什么？

- LLVM IR 基本组成是什么？

#### Lv.2：LLVM IR 手撕

~~~
常见的icmp
eq     ==
ne     !=

sgt    signed >
sge    signed >=
slt    signed <
sle    signed <=

ugt    unsigned >
uge    unsigned >=
ult    unsigned <
ule    unsigned <=


~~~



Lv.3：SSA 深挖

~~~
SSA
↓
Dominator
↓
Immediate Dominator
↓
Dominator Tree
↓
Dominance Frontier
↓
PHI insertion
↓
Rename

~~~

~~~
Dominator
PHI 放哪里
SSA 怎么构造

~~~

Lv.4：优化手撕

Lv.5：LLVM Backend

~~~
DAG / GlobalISel
Instruction Selection
MachineInstr
Virtual Register
Physical Register
Register Allocation
Graph Coloring
Linear Scan
Spilling
Instruction Scheduling
Calling Convention
TableGen

~~~

Lv.6：源码/项目追问